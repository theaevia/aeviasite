# Copy to .env and fill in values

# OAuth (Dev or Prod)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# If your repo is private, scopes must include 'repo'
# GITHUB_SCOPES=repo

# Optional: override OAuth redirect base (useful for tunnels)
# OAUTH_BASE_URL=http://localhost:3000

