# Copy to .env and fill in values

# Netlify Identity (for Decap CMS git-gateway)
# Provide your Netlify site Identity endpoint to enable login in dev/prod
# Example: https://your-site.netlify.app/.netlify/identity
# PUBLIC_NETLIFY_IDENTITY_URL=
